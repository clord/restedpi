{
  "name": "restedpi-fe",
  "version": "1.0.0",
  "description": "front-end interaction with the rpi core",
  "main": "src/index.tsx",
  "dependencies": {
    "esbuild": "^0.8.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "typescript": "^4.0.5"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.tsx --bundle --minify --sourcemap --target=chrome58,firefox57,safari11,edge16  --outfile=bundle.js"
  },
  "author": "Christopher C Lord"
}
